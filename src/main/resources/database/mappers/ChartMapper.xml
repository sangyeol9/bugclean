<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

  
  
  <mapper namespace="com.winter.app.chart.ChartDAO">
	<select id="getPrice">
		SELECT sum(price) AS result FROM SITE_SCH ss  WHERE TO_CHAR(START_TIME,'YYYY-MM') LIKE TO_CHAR(SYSDATE,('YYYY'))|| '-' || #{month}
	</select>
  </mapper>